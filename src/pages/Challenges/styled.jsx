import styled from "styled-components";
import { Container } from "../../common/styled";

export const ChallengesContainer = styled(Container)`
  display: flex;
  flex-wrap: wrap;
  gap: 2rem;
  justify-content: center;
`